left
<% if(mer.length!==0) { %>
<% row=Math.ceil(mer.length/3) %>
<% if(row == 1) { %>


<nav class="columns">
	<% for(var k=0;k<mer.length;k++ ) { %>
		
        <div class="column is-one-quarter has-text-centered has-background-ali" >
		<img src="img/cd/png/<%= mer[k].sku %>.png">
		</div><!--/col-->
	
	    <div class="column">
		    <%=shop.tit %>:<%= mer[k].name %><br>
		    <%=shop.sku %>:<%= mer[k].sku %><br>
		    <%=shop.pri %>:<%= mer[k].pri %><br>
		    <%=shop.des %>:<%= mer[k].des %><br>
		</div><!--/col-->
	
	<% } %><!-- for-->
</nav>
<% } else if(row==2 ) { %>

<nav class="columns">
	<% for(var k=0;k<3;k++ ) { %>
		<div class="column">
		<img src="img/cd/png/<%= mer[k].sku %>.png">
		</div><!--/col-->
	<% } %><!-- for-->
</nav>

<nav class="columns">
<% for(var k=3;k<6;k++ ) { %>
	<div class="column">
	<img src="img/cd/png/<%= mer[k].sku %>.png">
	</div><!--/col-->
<% } %>
</nav>
<% } else if(row==3 ) { %>


<nav class="columns">
	<% for(var k=0;k<3;k++ ) { %>
		<div class="column">
		<img src="img/cd/png/<%= mer[k].sku %>.png">
		</div><!--/col -->
	<% } %><!-- for-->
</nav>

<nav class="columns">
	<% for(var k=3;k<6;k++ ) { %>
		<div class="column">
		<img src="img/cd/png/<%= mer[k].sku %>.png">
		</div><!--/col-->
	<% } %>
</nav>

<nav class="columns">
	<% for(var k=6;k<mer.length;k++ ) { %>
		<div class="column">
		<img src="img/cd/png/<%= mer[k].sku %>.png">
		</div><!--/col-->
	<% } %>
</nav>


<% } %>
<% } else { %> <%=shop.noit %> <% }  %>


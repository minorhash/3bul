<% if(usr) { %>
welcome 
<%= usr.name%><%=shop.sama%>

<% include par/out%>

<% if( niq){ %>
<% for(var i=0;i<niq.length;i++){%>
<% if(niq[i]!=null && niq[i]!=undefined && niq[i]!=={}){%>
<%=shop.sku%>:tms-<%= niq[i] %><br>

<%} %><!-- if-->
<%} %><!-- for-->
<%} else {%>no niq<%} %>

<%} else {%>
<% include ./in%>
<%} %>
